node_modules
dist
release
.env
.env.local
*.local

# Personal Claude Code settings (shared config lives in .claude/launch.json)
.claude/settings.local.json

# Stray dev-time data dump (mangled C:\Temp\ path that landed in the repo root)
*legacy_raw.json

# Supabase CLI local state — records which project this checkout is linked to,
# which is per-machine and not part of the project.
supabase/.temp/

# Test runner build output
node_modules/.test-build/
