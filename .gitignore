node_modules
dist
release
.env
.env.local
*.local

# Personal Claude Code settings (shared config lives in .claude/launch.json)
.claude/settings.local.json

# Stray dev-time data dump (mangled C:\Temp\ path that landed in the repo root)
*legacy_raw.json

# Supabase CLI local state — records which project this checkout is linked to,
# which is per-machine and not part of the project.
supabase/.temp/

# Test runner build output
node_modules/.test-build/

# Dune spice deck — fifteen cards, each embedding a full copy of the board, so
# a single change to the map rewrites all of them and >1MB of diff with it.
# Regenerated exactly by scripts/build-dune-board.mjs; the board SVG and the
# data module ARE committed, because those are what the app loads.
public/dune-cards/
